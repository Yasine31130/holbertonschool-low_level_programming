#include<stdio.h>
#include<stdlib.h>
/**
 * main - description
 * @argc: num of arguments
 * @argv: array of arguments
 * Return: program that prints its name, followed by a new line
 */
int main(int argc, char  *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
